@model ProjectViewModel

@{
    ViewData["Title"] = $"{Model.ProjectName} - Team Members";
    var employees = Model.AssignedEmployees ?? Enumerable.Empty<ERP.DAL.Models.Employee>();
}

<div class="container-fluid px-4">
    <div class="page-header animate-in">
        <div class="container-fluid px-4">
            <div class="d-flex align-items-center gap-3 mb-2">
                <a asp-action="Index" class="btn btn-secondary btn-sm">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 12H5M12 19l-7-7 7-7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                    </svg>
                </a>
                <div>
                    <h1 class="mb-0">@Model.ProjectName</h1>
                </div>
            </div>
            <p>Viewing all team members assigned to this project</p>
        </div>
    </div>

    <!-- Project Info Card -->
    <div class="row g-4 mb-4 animate-in">
        <div class="col-lg-12">
            <div class="card" style="background: linear-gradient(135deg, #6366f1 0%, #818cf8 100%); color: white;">
                <div class="card-body p-4">
                    <div class="row align-items-center">
                        <div class="col-md-8">
                            <div class="d-flex align-items-center gap-3 mb-3">
                                <div class="d-flex align-items-center justify-content-center" style="width: 64px; height: 64px; background: rgba(255, 255, 255, 0.2); border-radius: var(--radius-md); flex-shrink: 0;">
                                    <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                        <rect x="2" y="7" width="20" height="14" rx="2" ry="2" stroke="white" stroke-width="2" />
                                        <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16" stroke="white" stroke-width="2" />
                                    </svg>
                                </div>
                                <div>
                                    <h3 class="mb-1">@Model.ProjectName</h3>
                                    <p class="mb-0 opacity-75">Project Code: @Model.ProjectCode</p>
                                    @if (Model.ProjectManager != null)
                                    {
                                        <p class="mb-0 opacity-75 mt-1">
                                            Manager: <strong>@Model.ProjectManager.FirstName @Model.ProjectManager.LastName</strong>
                                        </p>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="text-md-end">
                                <h2 class="mb-1">@employees.Count()</h2>
                                <p class="mb-0 opacity-75">Team Members</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (employees.Any())
    {
        <div class="table-container animate-in">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th style="width: 60px;">Photo</th>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Position</th>
                        <th>Department</th>
                        <th>Contact</th>
                        <th>Status</th>
                        <th class="text-center" style="width: 150px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var employee in employees.OrderBy(e => e.LastName))
                    {
                        <tr>
                            <td>
                                <img src="@employee.ImageUrl"
                                     alt="@employee.FirstName @employee.LastName"
                                     style="width: 40px; height: 40px; object-fit: cover; border-radius: 50%; border: 2px solid var(--gray-200);" />
                            </td>
                            <td>
                                <span class="table-code">@employee.Id</span>
                            </td>
                            <td>
                                <div>
                                    <strong>@employee.FirstName @employee.LastName</strong>
                                    <br>
                                    <small class="text-muted">@employee.Email</small>
                                </div>
                            </td>
                            <td>
                                <span class="badge-custom">@employee.Position</span>
                            </td>
                            <td>
                                @if (employee.Department != null)
                                {
                                    <div>
                                        <span class="table-code" style="font-size: 0.75rem;">@employee.Department.DepartmentCode</span>
                                        <br>
                                        <small class="text-muted">@employee.Department.DepartmentName</small>
                                    </div>
                                }
                            </td>
                            <td>
                                <small class="text-muted">@employee.PhoneNumber</small>
                            </td>
                            <td>
                                @if (employee.IsActive)
                                {
                                    <span class="badge" style="background-color: #dcfce7; color: #166534; padding: 0.375rem 0.75rem; border-radius: 0.375rem; font-weight: 500; font-size: 0.8125rem;">
                                        Active
                                    </span>
                                }
                                else
                                {
                                    <span class="badge" style="background-color: #f3f4f6; color: #4b5563; padding: 0.375rem 0.75rem; border-radius: 0.375rem; font-weight: 500; font-size: 0.8125rem;">
                                        Inactive
                                    </span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-controller="Employee" asp-action="Edit" asp-route-id="@employee.Id" class="btn btn-sm btn-outline-secondary" title="Edit Employee">
                                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                        <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" stroke="currentColor" stroke-width="2" />
                                        <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" stroke="currentColor" stroke-width="2" />
                                    </svg>
                                    Edit
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="card animate-in">
            <div class="empty-state">
                <div class="empty-state-icon">
                    <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" stroke="currentColor" stroke-width="2" />
                        <circle cx="9" cy="7" r="4" stroke="currentColor" stroke-width="2" />
                    </svg>
                </div>
                <h5>No Team Members Assigned</h5>
                <p>This project doesn't have any team members assigned yet.</p>
                <a asp-controller="Employee" asp-action="Index" class="btn btn-primary btn-primary-lg">
                    <span>View All Employees</span>
                </a>
            </div>
        </div>
    }
</div>